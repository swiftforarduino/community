These are example libraries you can add to your projects. Simply go to Import Tab... and add any of these files to your build.



*footnotes for contributors*
This is currently the best place to add sample code libraries, when they are complete. Use work in progress for libraries that you don't yet consider complete or that shouldn't be used by a novice.
use the shell script or sed file to automatically create an accompanying
.txt documentation file, or write/modify by hand

note: the file catalog.txt determines which files are downloaded. if a file is not listed in there, it is not downloaded automatically